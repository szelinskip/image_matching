#pragma once

#include <string>

namespace utils {
namespace str {

std::string replace(const std::string& target, const std::string& search, const std::string& replace);

} // namespace str
} // namespace utils
